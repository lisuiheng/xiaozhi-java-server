<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.lisuiheng.astra.sys.mapper.SysNoticeMapper">

    <!-- 通用结果列 -->
    <resultMap id="SysNoticeResultMap" type="com.github.lisuiheng.astra.sys.domain.vo.SysNoticeVo">
        <id property="noticeId" column="notice_id"/>
        <result property="noticeTitle" column="notice_title"/>
        <result property="noticeType" column="notice_type"/>
        <result property="noticeContent" column="notice_content"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createBy" column="create_by"/>
        <result property="createByName" column="create_by_name"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 分页查询通知公告列表 -->
    <select id="selectVoPage" resultMap="SysNoticeResultMap">
        SELECT notice_id,
               notice_title,
               notice_type,
               notice_content,
               n.status,
               n.remark,
               n.create_by,
               u.user_name AS create_by_name,
               n.create_time
        FROM sys_notice n
        LEFT JOIN sys_user u ON n.create_by = u.user_id
        ${ew.customSqlSegment}
    </select>

    <!-- 根据ID查询通知公告详情 -->
    <select id="selectVoById" parameterType="java.lang.Long" resultMap="SysNoticeResultMap">
        SELECT notice_id,
               notice_title,
               notice_type,
               notice_content,
               n.status,
               n.remark,
               n.create_by,
               u.user_name AS create_by_name,
               n.create_time
        FROM sys_notice n
        LEFT JOIN sys_user u ON n.create_by = u.user_id
        WHERE notice_id = #{noticeId}
    </select>

    <!-- 查询通知公告列表 -->
    <select id="selectVoList" resultMap="SysNoticeResultMap">
        SELECT notice_id,
               notice_title,
               notice_type,
               notice_content,
               n.status,
               n.remark,
               n.create_by,
               u.user_name AS create_by_name,
               n.create_time
        FROM sys_notice n
        LEFT JOIN sys_user u ON n.create_by = u.user_id
        ${ew.customSqlSegment}
    </select>

</mapper>